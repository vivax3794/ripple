[workspace]
resolver = "2"
members = ["ripple_parser", "surf"]
# lto = "fat"
# strip = true
# codegen-units = 1

[profile.dev.package."*"]
opt-level = 3

[profile.release]
codegen-units = 1
lto = "fat"
panic = "abort"
strip = "debuginfo"
