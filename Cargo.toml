[workspace]
resolver = "2"
members = ["ripple", "ripple_macros", "test_project"]

[profile.dev.package."*"]
opt-level = 3

[profile.release]
lto = "fat"
codegen-units = 1
opt-level = "z"
strip = true
panic = "abort"
